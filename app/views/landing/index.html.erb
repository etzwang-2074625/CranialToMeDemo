<%= render partial: 'nav' %>
<%= render partial: 'main' %>

<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    /* font-family: ; */
}

body {
    background: #015e87 repeat-y 100px 100px / 120px 80px fixed;
    background-image: url('/assets/landing/bg-min.webp');
    width: 100%;
    background-size: cover;
    background-position: center;
    overflow-x: hidden;
    background-repeat: no-repeat;
    position: relative;
    margin-top: -6em;
    flex-direction: row;
}

header {
    position: fixed;
    top: 15px;
    width: 90%;
    z-index: 100;
}

nav {
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-radius: 10px;
    padding: 5 25px;
}

nav .logo {
    width: 50vw;
}

nav .logo img {
    cursor: pointer;
}

nav ul {
    display: flex;
    gap: 30px;
    list-style: none;
}
.nav-link{
    font-size: 20px;
    border-radius: 20px;
}

nav ul li a {
    font-size: 18px;
    color: #fff;
}

.toggle {
    color: #fff;
    cursor: pointer;
    font-weight: bold;
    transform: rotate(90deg);
    display: none;
    user-select: none;
}

.button {
    margin: 10px;
    background: #1E98D7;
    color: #fff;
    outline: none;
    border: 0;
    font-weight: 600;
    font-size: 16px;
    border-radius: 30px;
    transition: 0.3s;
}
.btn{
    padding: 12px 21px !important;
    margin: 10px;
    font-weight: 600;
    font-size: 16px;
    border-radius: 30px;
    transition: 0.3s;
}
.btn:hover {
    background: #11709C;
    color: #eee;
}

.button:hover {
    background: #11709C;
    color: #eee;
}

.main {
    max-width: 80%;
    margin: auto;
    min-height: 102vh;
    display: flex;
    align-items: center;
}

.main-area{
    overflow: auto;
}

.main .icon img {
    transition: 0.05s;
}

.main .icon img:hover {
    scale: 1.2;
}

</style>
